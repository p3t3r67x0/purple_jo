# Example environment overrides for extract_header.service
POSTGRES_DSN=postgresql+asyncpg://user:pass@db-host:5432/netscanner
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
QUEUE_NAME=http_headers
WORKER_PROCESSES=4
CONCURRENCY=200
PREFETCH=400
REQUEST_TIMEOUT=5.0
SCHEMES=http,https
# e.g. '--verbose --log-headers'
EXTRA_ARGS=
